package nsolutions.space.huw.domain.model.team;

import java.util.UUID;

public class Manager extends Worker {

	public Manager(UUID tenantID) {
		super(tenantID);
	}

	
	// can create work order
	
	// can create spools and welds for them
	
	// can assign workers to work order
	
}
