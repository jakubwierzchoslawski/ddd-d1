package nsolutions.space.huw.domain.model.team;

import java.util.UUID;

public class Welder extends Worker {

	public Welder(UUID tenantID) {
		super(tenantID);
	}
	

	// can create spool and welds
	
	// can do measurements for welds
	
	// can attach photos to measurement, one photo per measurement
	
	// can worker assign himself to work order?
}
